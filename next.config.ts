import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Images autorized all src
  images: {
    remotePatterns: [
      {
        protocol: "http",
        hostname: "**",
      },
      {
        protocol: "https",
        hostname: "**",
      },
    ],
  },
};

export default nextConfig;
